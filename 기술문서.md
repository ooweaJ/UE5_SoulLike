# SoulLike - 서버 권위 액션 구조 현대화 기술문서

## 1. 프로젝트 한 줄 소개

Unreal Engine 5.7 기반 소울라이크 액션 RPG 프로젝트에서 기존 `Server RPC -> Reliable Multicast` 중심 전투 흐름을 서버 권위 액션 상태 복제 구조로 단계적으로 현대화한 개인 프로젝트입니다.

이 문서는 게임의 그래픽 완성도보다 **멀티플레이 전투 동기화, 액션 상태 복제, AI/보스 스킬 권한 분리, 검증 가능한 리팩터링 과정**을 설명하기 위한 포트폴리오 기술문서입니다.

## 2. 목표와 제약

### 목표

- 플레이어와 AI 액션이 서버 기준으로 결정되고, 클라이언트에는 복제 상태 또는 명시적인 네트워크 이벤트로 표현되게 만든다.
- 평타, 피격, 장착 아이템, 보스 스킬, 사망/부활처럼 전투 핵심 흐름에서 서버/클라이언트 결과가 어긋나는 문제를 줄인다.
- 기존 프로젝트를 한 번에 갈아엎지 않고, 빌드 가능한 작은 단위로 수정하고 문서화한다.

### 제약

- 이미 구현된 레거시 Unreal 프로젝트를 현대화하는 작업이므로 대규모 재작성 대신 단계적 수정이 필요했다.
- `AItem`, Anim Notify, Character, Controller가 서로 직접 호출하는 구조가 남아 있어 한 번의 변경으로 전체 액션 시스템을 교체하기 어려웠다.
- Steam Online Subsystem 전환 준비는 있었지만, 게임플레이 복제 문제와 OSS 문제를 분리하기 위해 NULL/직접 접속 및 PIE 검증을 우선했다.

## 3. 직접 구현 범위

### 직접 설계 및 구현

- 액션 실행 단일 진입점 `UActionComponent::TryAction(FGameplayTag)` 추가
- 소유 클라이언트 예측 재생과 서버 확정 액션 상태 복제 구조 설계
- `FActiveAction { ActionTag, Sequence }` 기반 액션 반복 복제 트리거 추가
- `UStateComponent::Type` 서버 권위 갱신 및 `OnRep` 기반 복제 전환
- 장착 아이템 Actor와 선택 아이템 참조 복제 기반 정리
- `AItem::CurrentActionTag` 복제로 클라이언트가 같은 DataTable 액션 데이터를 재구성하도록 수정
- 피격 리액션을 서버 판정 후 Multicast 이벤트로 전파하도록 수정
- Grux 보스 메테오/공중 콤보의 서버 권위 처리와 널 가드 보강
- 플레이어 사망/부활 흐름에서 서버 상태 복구와 클라이언트 표현 복구 분리
- 별도 프로세스 NULL 리슨서버/클라이언트 테스트 스크립트 작성
- 작업 로그, 아키텍처 문서, 검증 기록 작성

### 외부 리소스 및 기존 프로젝트 자산

- 캐릭터, 애니메이션, Niagara 이펙트, 일부 맵/에셋은 기존 프로젝트 리소스와 외부 에셋을 사용했다.
- 포트폴리오 평가 대상은 에셋 제작이 아니라 전투 액션 구조, 네트워크 동기화, 디버깅 및 리팩터링 과정이다.

### AI 활용 범위

- Codex를 코드 검색, 영향 범위 추적, 작업 로그 정리, 테스트 기준 문서화에 활용했다.
- UE-MCP를 사용해 에디터 상태 확인, PIE 실행, 런타임 Pawn/Component 상태 확인을 보조했다.
- 최종 구조 선택, 코드 수정 방향, 빌드 실패 원인 판단, 검증 기준 결정은 직접 수행했다.

## 4. 사용 기술

| 구분 | 기술 | 사용 목적 |
| --- | --- | --- |
| Engine | Unreal Engine 5.7 | C++ 기반 액션 RPG, PIE/별도 프로세스 검증 |
| Language | C++ | 캐릭터, 액션, 아이템, AI, 투사체, 네트워크 로직 구현 |
| Networking | Unreal Replication, RPC, NetMulticast | 액션 상태, 장착 아이템, 피격/이펙트 이벤트 동기화 |
| Gameplay | GameplayTag, DataTable, Anim Notify | 액션 식별, 액션 데이터 조회, 몽타주 타이밍 이벤트 처리 |
| AI | Behavior Tree, AIController, Blackboard | AI 타겟 탐색, 공격 의사결정, 보스 스킬 실행 |
| Online | OnlineSubsystem NULL/Steam 준비 | 로컬 직접 접속 테스트와 Steam 세션 전환 준비 |
| Tooling | UnrealBuildTool, UE-MCP, Git | 빌드 검증, PIE 확인, 변경 단위 기록 |

## 5. 기존 구조의 핵심 문제

초기 전투 흐름은 액션마다 네트워크 패턴이 달랐다.

```text
Input
  -> ABasePlayer::OnMouseL / OnMouseR / OnQ / OnEvade
  -> Server RPC
  -> Reliable NetMulticast
  -> UEquipComponent::GetCurrentItem()
  -> AItem::OnDefaultAction / OnSkillAction
  -> AnimMontage 재생
  -> Anim Notify에서 콤보, 충돌, 이동, 액션 종료 처리
```

이 구조에서는 다음 문제가 발생할 수 있었다.

- 소유 클라이언트도 서버 왕복 후에야 평타 몽타주를 보게 되어 지연 환경에서 입력감이 떨어진다.
- 클라이언트가 `StateComponent::Type` 같은 복제 변수를 로컬에서 직접 변경할 수 있어 서버 상태와 충돌할 수 있다.
- `EquipComponent::SelectItem`, `AItem::CurrentData`가 서버/클라이언트에서 같은 액션을 가리킨다는 보장이 부족했다.
- AIController는 서버에만 존재하므로, AIController에서 결정한 애니메이션 속도나 공격 타이밍을 클라이언트가 그대로 재현하기 어렵다.
- Anim Notify가 판정, 이동, 콤보 진행, 액션 종료까지 직접 호출해 책임 경계가 흐렸다.

## 6. 목표 구조

전투 액션의 목표 흐름은 다음과 같다.

```text
Client Input
  -> UActionComponent::TryAction(ActionTag)
  -> Owner Client Prediction
  -> Server_TryAction(ActionTag)
  -> Server Validation
  -> FActiveAction { ActionTag, Sequence } 갱신
  -> OnRep_ActiveAction()
  -> 각 클라이언트에서 동일 ActionTag 기반 몽타주 재생
```

핵심 원칙은 네 가지다.

- 입력은 클라이언트에서 시작할 수 있지만, 최종 액션 상태는 서버가 확정한다.
- 클라이언트는 서버가 복제한 액션 태그를 기준으로 같은 DataTable 액션 데이터를 재구성한다.
- 소유 클라이언트는 입력감을 위해 즉시 예측 재생하되, 서버 확정 상태와 충돌하지 않도록 owner 복제는 건너뛴다.
- 데미지, 사망, 부활, 보스 스킬 판정처럼 결과가 중요한 처리는 서버 권위로 제한한다.

## 7. 핵심 구현

### 7-1. 통일 액션 컴포넌트

`UActionComponent`는 평타 경로를 먼저 이전하기 위한 최소 액션 레이어다.

주요 코드 경로:

- `Source/SoulLike/Component/ActionComponent.h`
- `Source/SoulLike/Component/ActionComponent.cpp`
- `Source/SoulLike/Actor/Character/Player/BasePlayer.cpp`
- `Source/SoulLike/Actor/Character/BaseCharacter.cpp`

핵심 구현:

- `TryAction(FGameplayTag)`를 액션 요청 단일 진입점으로 사용한다.
- 서버에서는 `CommitAction()`으로 검증과 실행을 처리한다.
- 소유 클라이언트는 `CanStartAction()`을 통과하면 즉시 `PlayActionLocal()`을 실행해 입력 지연을 줄인다.
- 서버 확정 액션은 `ActiveAction`으로 복제한다.
- `ActiveAction`은 `COND_SkipOwner`로 복제해 예측 재생한 소유 클라이언트의 중복 재생을 피한다.
- `Sequence`를 증가시켜 같은 ActionTag가 반복되어도 `OnRep`가 다시 실행될 수 있게 한다.

### 7-2. 상태 복제 기준 정리

`UStateComponent::Type`은 기존에 복제 변수였지만, 클라이언트 로컬에서 직접 변경될 여지가 있었다.

수정 방향:

- `Type`을 `ReplicatedUsing=OnRep_Type` 기준으로 정리했다.
- `ChangeType()`은 서버 권위에서만 상태를 바꾸도록 제한했다.
- 액션 종료 시 서버에서 `Idle`로 복귀시키고, 클라이언트 예측 잠금은 몽타주 종료 기준으로 해제하도록 분리했다.

이 변경의 목적은 클라이언트가 임의로 액션 상태를 앞서 변경해 다음 서버 복제와 충돌하는 상황을 줄이는 것이다.

### 7-3. 장착 아이템 및 현재 액션 태그 복제

기존 액션/Notify 구조는 `EquipComponent::GetCurrentItem()`과 `AItem::CurrentData`에 크게 의존했다. 따라서 서버와 클라이언트가 서로 다른 아이템 또는 액션 데이터를 보고 있으면 같은 몽타주라도 판정과 콤보가 어긋날 수 있었다.

주요 코드 경로:

- `Source/SoulLike/Component/EquipComponent.h`
- `Source/SoulLike/Component/EquipComponent.cpp`
- `Source/SoulLike/Actor/Item/Item.h`
- `Source/SoulLike/Actor/Item/Item.cpp`

핵심 구현:

- 기본 아이템은 서버에서만 스폰한다.
- `UEquipComponent::SelectItem`을 `ReplicatedUsing=OnRep_SelectItem`으로 전환한다.
- `AItem`을 복제 Actor로 전환하고 `bAlwaysRelevant`와 NetUpdateFrequency를 조정한다.
- `AItem::CurrentActionTag`를 복제하고, `OnRep_CurrentActionTag()`에서 로컬 DataTable을 다시 조회해 `CurrentData`를 재구성한다.

이 방식은 `FActionData` 전체를 복제하지 않고, 액션의 의미를 나타내는 GameplayTag만 복제해 네트워크 상태를 작게 유지하는 선택이다.

### 7-4. 피격 리액션 전파

기존에는 서버에서 데미지를 처리해도 클라이언트에 피격 몽타주 재생 호출이 전달되지 않을 수 있었다.

수정 방향:

- `ABaseCharacter::TakeDamage()`는 서버에서 데미지/사망 판단을 처리한다.
- 피격 몽타주 키 선택은 `UMontageComponent`로 이동한다.
- `ABaseCharacter::MulticastPlayHitReaction()`으로 클라이언트에 피격 리액션 이벤트를 전파한다.

결과적으로 데미지 판정은 서버가 담당하고, 각 클라이언트는 같은 피격 리액션을 재생하는 구조가 되었다.

### 7-5. Grux 메테오와 보스 스킬 동기화

보스 Grux의 메테오와 공중 콤보는 서버/클라이언트가 서로 다른 상태를 만들기 쉬운 흐름이었다.

주요 코드 경로:

- `Source/SoulLike/Actor/ProJectile/GruxMeteor.h`
- `Source/SoulLike/Actor/ProJectile/GruxMeteor.cpp`
- `Source/SoulLike/Actor/Character/AI/Grux.cpp`
- `Source/SoulLike/Actor/Item/GruxItem.cpp`

수정 내용:

- `AGruxMeteor`를 복제 Actor로 전환하고 이동 복제를 켰다.
- Target, Damage 같은 메테오 상태를 복제 속성으로 추가했다.
- 타이머 시작, 투사체 활성화, 충돌, 데미지, 파괴는 서버 권위에서만 실행되도록 제한했다.
- 바닥 폭발과 캐릭터 직접 피격을 분리했다.
- 폭발 이펙트는 서버에서 Multicast로 전파하고, Actor 삭제는 짧게 지연해 이펙트 RPC가 사라지지 않게 했다.
- Grux 공중 콤보 관련 함수에 서버 권위 가드와 널 가드를 추가했다.

### 7-6. 사망/부활 흐름 보정

플레이어 사망 완료가 Anim Notify에만 의존하면 Notify 누락 시 부활 타이머가 시작되지 않을 수 있었다. 또한 서버 권위 상태 복구와 클라이언트 표현 복구가 한 함수에 섞여 있었다.

수정 방향:

- 사망 완료 처리는 서버에서 한 번만 예약한다.
- 사망 Multicast는 visibility와 사망 이펙트 같은 표현만 처리한다.
- 3초 후 서버 타이머가 부활 Multicast를 호출한다.
- HP 회복과 `State->SetIdleMode()`는 authority 인스턴스에서만 수행한다.
- 입력, visibility, 몽타주 정리, 이동 가능 상태 복구는 각 클라이언트 표현에 맞게 처리한다.
- GameMode 리스폰 타이머는 함수 로컬 핸들로 사용해 여러 플레이어 타이머가 덮이지 않도록 했다.

## 8. 문제 해결 사례

### 사례 1. 소유 클라이언트 평타 입력 지연

문제: 기존 평타는 `Server RPC -> Reliable Multicast` 이후에 몽타주가 재생되어, 지연 환경에서는 소유 클라이언트 입력 반응이 늦어질 수 있었다.

원인: 소유 클라이언트도 서버 응답을 기다린 뒤 같은 Multicast 경로로 표현을 받는 구조였다.

해결: `UActionComponent::TryAction()`에서 소유 클라이언트가 조건을 통과하면 즉시 로컬 예측 재생을 하고, 서버에는 `Server_TryAction()`을 보냈다. 서버 확정 액션은 `ActiveAction`으로 복제하되 owner에는 `COND_SkipOwner`를 적용했다.

결과: 평타 경로에서 입력 즉시 반응하는 구조와 서버 권위 액션 확정 구조를 동시에 둘 수 있는 기준선이 생겼다.

### 사례 2. 장착 아이템/현재 액션 데이터 불일치

문제: Anim Notify와 콤보 로직이 현재 아이템과 현재 액션 데이터에 의존하지만, 서버와 클라이언트가 같은 데이터를 보고 있다는 보장이 부족했다.

원인: 장착 아이템 참조와 `CurrentData` 전체가 네트워크 기준으로 안정화되어 있지 않았다.

해결: 선택 아이템 Actor를 서버 스폰/복제 구조로 정리하고, 현재 액션은 `FActionData` 전체 대신 `CurrentActionTag`를 복제해 클라이언트가 DataTable에서 같은 액션 데이터를 재구성하도록 했다.

결과: 액션 의미를 GameplayTag로 맞추는 최소 복제 구조가 생겼고, 이후 콤보 인덱스/몽타주 섹션/시작 시간 같은 상태를 확장할 수 있는 기반이 마련되었다.

### 사례 3. 보스 메테오 이펙트와 판정 불일치

문제: Grux 메테오가 클라이언트에서 안정적으로 보이지 않거나, 충돌 직후 Actor가 삭제되어 폭발 이펙트 Multicast를 놓칠 수 있었다.

원인: 메테오 Actor 복제와 서버 권위 충돌 처리가 부족했고, Multicast 직후 즉시 `Destroy()`가 호출되는 구조였다.

해결: 메테오를 복제 Actor로 전환하고 서버에서만 충돌/데미지/파괴를 처리했다. 이펙트 Multicast 후에는 충돌과 이동을 끄고 `SetLifeSpan(1.f)`로 지연 삭제했다.

결과: 보스 스킬의 실제 판정은 서버가 결정하고, 클라이언트는 복제 Actor와 Multicast 이펙트를 관찰하는 구조로 정리되었다.

### 사례 4. 사망 후 부활/입력 복구 불안정

문제: 사망 Notify가 누락되거나 타이밍이 어긋나면 부활 타이머가 시작되지 않거나, 부활 후 입력과 이동 가능 상태가 복구되지 않을 수 있었다.

원인: 사망 완료, 부활 타이머, HP/State 복구, visibility/input 복구가 같은 흐름에 섞여 있었고 일부 처리가 클라이언트에서도 직접 실행될 수 있었다.

해결: 서버가 사망 완료와 부활 타이머를 한 번만 관리하고, 클라이언트는 Multicast를 통해 표현 상태를 복구하도록 분리했다. 부활 시 `Status->SetMove()`도 호출해 이동 가능 상태를 되돌렸다.

결과: 사망/부활의 서버 권위 상태와 클라이언트 표현 책임이 더 명확해졌다.

## 9. 검증 결과

### 빌드 검증

다음 UnrealBuildTool 빌드를 반복적으로 수행했다.

```text
C:\Program Files\Epic Games\UE_5.7\Engine\Build\BatchFiles\Build.bat SoulLikeEditor Win64 Development -Project=C:\jaewoo\Unreal\UE5_SoulLike\SoulLike.uproject -WaitMutex -FromMsBuild
```

확인 결과:

- `SoulLikeEditor Win64 Development` 빌드 성공
- 작업 로그 기준 여러 수정 단위에서 `Result: Succeeded` 확인
- UHT 선언 오류, 타입 불일치, 접근 지정자 오류, deprecation 경고를 빌드 로그 기준으로 수정

### PIE 검증

2인 PIE에서 다음을 확인했다.

- `BP_Warrior0`, `BP_Warrior1` 스폰 확인
- 두 Pawn 모두 `StateComponent.Type=Idle`
- 두 Pawn 모두 `StatusComponent.SP.Current=600`
- `EquipComponent.SelectItem` 유효 상태 확인
- `ActionComponent.ActiveAction.Sequence=0` 기본 상태 확인
- PIE 로그에서 `Error`, `Warning` 필터 결과 없음

### 별도 프로세스 검증 준비

`Scripts/LaunchNullListenTest.bat`를 작성해 다음 구성을 준비했다.

```text
Listen Server: /Game/_dev/Level/MainWorld?listen
Client: 127.0.0.1 직접 접속
Option: -nosteam
```

목적:

- Steam OSS 전환 전에 NULL/직접 접속 기준으로 전투 동기화 문제를 분리한다.
- 에디터 PIE와 별도 프로세스 환경의 Possess, BeginPlay, 복제 타이밍 차이를 확인한다.

## 10. 핵심 코드 경로

| 목적 | 코드 경로 |
| --- | --- |
| 통일 액션 진입점 | `Source/SoulLike/Component/ActionComponent.h`, `Source/SoulLike/Component/ActionComponent.cpp` |
| 플레이어 평타 입력 연결 | `Source/SoulLike/Actor/Character/Player/BasePlayer.cpp` |
| 공통 캐릭터 컴포넌트 구성 | `Source/SoulLike/Actor/Character/BaseCharacter.cpp` |
| 장착 아이템 복제 | `Source/SoulLike/Component/EquipComponent.h`, `Source/SoulLike/Component/EquipComponent.cpp` |
| 현재 액션 태그 복제 | `Source/SoulLike/Actor/Item/Item.h`, `Source/SoulLike/Actor/Item/Item.cpp` |
| 피격 리액션 전파 | `Source/SoulLike/Actor/Character/BaseCharacter.cpp`, `Source/SoulLike/Component/MontageComponent.cpp` |
| 보스 메테오 동기화 | `Source/SoulLike/Actor/ProJectile/GruxMeteor.h`, `Source/SoulLike/Actor/ProJectile/GruxMeteor.cpp` |
| 사망/부활 처리 | `Source/SoulLike/Actor/Character/Player/BasePlayer.cpp`, `Source/SoulLike/Actor/GameMode/MainWorldGameMode.cpp` |
| 별도 프로세스 테스트 | `Scripts/LaunchNullListenTest.bat`, `Scripts/LaunchSteamMenuTest.bat` |

## 11. 회고와 남은 작업

### 잘한 점

- 기존 구조를 한 번에 갈아엎지 않고, 평타 액션부터 서버 권위 액션 상태 복제 구조를 적용했다.
- 장착 아이템, 현재 액션 태그, 피격 리액션, 보스 메테오, 사망/부활처럼 실제 동기화 문제가 생기는 지점을 작게 나누어 수정했다.
- 코드 변경마다 작업 로그와 검증 결과를 남겨 포트폴리오에서 “무엇을 고쳤는지”뿐 아니라 “왜 그렇게 고쳤는지”를 설명할 수 있게 했다.

### 한계

- 현재 `UActionComponent`는 평타 경로 중심의 1차 기준선이며, 회피/스텝백/스킬/AI 액션 전체가 같은 구조로 완전히 이전된 상태는 아니다.
- 콤보 인덱스, 몽타주 시작 시간, 섹션, 재생 속도는 아직 별도 액션 상태로 완전히 정규화되지 않았다.
- Steam OSS 런타임 세션 검증은 준비 단계이며, 실제 두 PC 또는 두 Steam 계정 환경 검증이 추가로 필요하다.
- Anim Notify는 여전히 일부 게임플레이 책임을 가지고 있어 장기적으로 타이밍 이벤트 역할로 더 축소해야 한다.

### 다음 개선

1. 회피, 스텝백, 스킬을 `UActionComponent::TryAction()` 패턴으로 이전한다.
2. AI 평타/스킬도 서버 확정 액션 상태 복제 구조로 통일한다.
3. `FActiveAction`에 콤보 인덱스, 몽타주 섹션, 시작 시간 또는 예측 보정에 필요한 최소 필드를 추가한다.
4. `Net PktLag` 환경에서 소유 클라이언트 예측 재생과 서버 보정 결과를 검증한다.
5. NULL 직접 접속 검증을 통과한 뒤 Steam OSS 세션 검증으로 넘어간다.

## 12. 포트폴리오 요약 문장

이 프로젝트에서 나는 **레거시 Unreal 액션 RPG의 전투 동기화 문제**를 해결하기 위해 `UActionComponent` 기반의 **서버 권위 액션 상태 복제 구조**를 설계했고, `GameplayTag`, `DataTable`, Unreal Replication, RPC를 사용해 **평타 예측 재생, 장착 아이템 복제, 현재 액션 태그 복제, 피격/보스 스킬/사망 부활 동기화**를 단계적으로 구현했다.

그 결과를 UnrealBuildTool 빌드, 2인 PIE 런타임 상태 확인, 별도 프로세스 NULL 테스트 스크립트, 날짜별 작업 로그와 코드 경로로 증빙할 수 있다.
